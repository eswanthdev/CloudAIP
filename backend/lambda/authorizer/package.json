{
  "name": "cloudaip-authorizer",
  "version": "1.0.0",
  "description": "JWT Lambda authorizer for CloudAIP API",
  "main": "index.js",
  "scripts": {
    "build": "esbuild index.ts --bundle --platform=node --target=es2022 --outfile=dist/index.js"
  },
  "dependencies": {
    "jsonwebtoken": "^9.0.2"
  },
  "devDependencies": {
    "@types/jsonwebtoken": "^9.0.6",
    "@types/aws-lambda": "^8.10.136",
    "typescript": "^5.4.3",
    "esbuild": "^0.20.2"
  }
}
